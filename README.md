# PSMASegmentator
Package for the automatic segmentation of PSMA PET images using AI.

## Installation Instructions
From the root directory of the package, run the following command: 
`pip install -e .`